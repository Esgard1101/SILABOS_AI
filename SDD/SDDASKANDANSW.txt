Tengo todo lo que necesito. Acá está el análisis de patrones de tus 4 sprints reales, y al final las 10 preguntas afinadas con lo que encontré.
Análisis de tus sprints pasados
Leí los 4 documentos completos (Linktree, Diócesis MVP, Diócesis Sprint 3, y el fork GesRest/Golden Brasas). No son inconsistentes por accidente: cada uno usa un formato distinto porque resuelven problemas distintos. Eso es justo lo que hay que estandarizar sin aplanar.
Patrón 1 — Tu granularidad cambia según greenfield vs brownfield, y lo haces bien intuitivamente.

Linktree (greenfield): tareas T1.1 → T3.3 con dependencias lineales estrictas. Funcionó porque era de cero y vos eras el único dev.
Diócesis MVP (greenfield, equipo): tareas T-01…T-13 agrupadas en 2 fases paralelas, con Asignado a: ESGARDO/CESAR. Acá ya aparece el reparto.
GesRest fork (brownfield): bloques BLOQUE 1…6 con TAREA → Subtarea, etiqueta [Dev A]/[Dev B], y una tabla "Decisiones tomadas" (Pregunta | Decisión) al inicio. Este es tu formato más maduro para proyectos avanzados.

Patrón 2 — El antipatrón que describís SÍ aparece, y es localizable.
En Diócesis MVP, T-01 mete las 14 migraciones + modelos + seeders en una sola tarea horizontal que bloquea toda la Fase 2. Ese es exactamente el "Tarea 1 = todas las tablas" que querés eliminar. Pero ojo: en ese contexto multi-dev tenía una lógica (un dev dueño del esquema). El problema no es la tarea grande en sí, es que no es testeable E2E por vos — no podés abrir el navegador y validar "una migración".
Patrón 3 — Ya tenés el slice vertical cuando el proyecto te deja.
En Diócesis, T-07 Bautismo, T-09 Matrimonio, T-10 Confirmación SÍ son slices verticales perfectos: cada uno es Migration → Model → Service → FormRequest → Controller → Blade (lista+form) → Partida PDF. Cada módulo es testeable solo. Esa es tu plantilla de oro y ni la nombraste como tal.
Patrón 4 — Tus "Requisitos de testeo" ya son criterios E2E, pero mezclás dos niveles.
A veces son verificables por vos en navegador ("abrir persona con bautismo+confirmación muestra los 3"), a veces son técnicos de tinker ("Bautismo::with(...) no lanza error"). Para tu objetivo "testeable end2end por mí", hay que separar DoD-Usuario (navegador) de DoD-Técnico (tinker/migrate).
Patrón 5 — El "círculo virtuoso" ya existe y funciona — Sprint 3 lo demuestra.
Tu context.md del Linktree tiene 20 filas de bitácora fechadas. Y el Sprint 3 de Diócesis es el mejor ejemplo: tareas que cambiaron de plan a mitad (T-3.A.3 CANCELADA, T-3.A.5 reformulada, T-3.B.5 POSTERGADA a Sprint 4) con la razón documentada inline. Tu auto-mejora ya está viva; solo falta estandarizar dónde va (¿en la tarea? ¿en la bitácora? hoy está en ambos lados).
Patrón 6 — La nomenclatura inconsistente que mencionás es real y rastreable.
Diócesis usa parroquia_id como tenant en sesión. Linktree usa empresa_id como FK pero sin tenant activo (una sola empresa). GesRest usa UUID de branch. Tres proyectos, tres conceptos de "scope", y el rol es rol_id en Diócesis pero "rol único sin tabla" en Linktree. Un agente sin glosario explícito va a alucinar cuál aplica — exactamente tu miedo del usuario_id vs perfil_id.
Patrón 7 — Las dependencias son tu verdadero cuello de botella, no el tamaño de tarea.
Los 4 docs tienen diagramas de dependencias ASCII. El dolor no es que las tareas sean grandes, es que están encadenadas (T1.1 bloquea T1.2 bloquea...). Para "no volver al primer thread", el objetivo no es solo verticalizar: es minimizar aristas de dependencia para que cada tarea arranque en sesión limpia con solo leer context.md + su SPEC.

Con esos patrones, acá van las 10 preguntas (afinadas, no genéricas). Te las hago de a una conceptualmente pero las listo todas para que elijas:
1. Umbral greenfield vs brownfield. Vi que ya usás SPRINT lineal para cero (Linktree) y BLOQUE+Subtarea para forks (GesRest). ¿Querés que la plantilla tenga dos modos explícitos (MODO-SPRINT para nuevo / MODO-SPEC para existente) y que el agente Capa 1 te pregunte al inicio cuál aplica, o preferís un formato único que escale a ambos?
2. La definición de "átomo testeable". Tu mejor slice (T-07 Bautismo) es Migration→…→Blade+PDF. ¿Ese es tu tamaño ideal de tarea (un módulo CRUD completo con su pantalla), o querés bajar aún más a "una pantalla interactuable" aunque el CRUD quede en 2 tareas (ej. "listado+lectura" y "alta+edición")?
3. El problema de T-01 (cimientos compartidos). En proyectos nuevos siempre hay una base no-verticalizable (migraciones núcleo, auth, layout). ¿Querés mantenerla como "Épica 0 / Cimientos" explícitamente horizontal y aceptada como excepción, o forzar que hasta el setup entregue algo visible (ej. "login funcional + dashboard vacío" como primer slice)?
4. DoD de dos niveles. ¿Confirmás que querés separar en cada tarea ✅ Testeable por mí (navegador) de 🔧 Verificación técnica (migrate/tinker), para que el primero sea tu checklist de aceptación real y el segundo sea responsabilidad del agente?
5. Glosario de Ubicuidad — alcance. Para matar el usuario_id/perfil_id/rol_id/tenant: ¿querés un glosario por proyecto (vive en context.md §2) que el SPEC referencie, o uno global tuyo (un GLOSARIO.md reusable entre proyectos con tus convenciones GarzaSoft fijas)?
6. Mockups en el flujo. Confirmaste que generás el HTML en chat aparte para no gastar tokens del agente de coding. ¿Querés que el SPEC tenga un campo obligatorio Mockup: resources/mockups/archivo.html [PENDIENTE|APROBADO] y que el agente Capa 1 se niegue a escribir el SPRINT hasta que el mockup esté APROBADO, o preferís que sea opcional según el módulo?
7. Reparto multi-dev. En Diócesis y GesRest etiquetás [ESGARDO]/[CESAR]/[Dev A]. ¿La plantilla debe siempre incluir campo Asignado a: y un mapa de qué módulos puede tocar cada dev (para tu regla "quien hace el módulo hace su menú" y evitar conflictos de archivos), o eso lo asignás vos manualmente fuera del doc?
8. Dónde vive la auto-mejora. Hoy documentás decisiones en 2 lugares: inline en la tarea (Cierre 2026-05-24:) y en context.md §Bitácora. ¿Querés estandarizar que la decisión se escribe inline en la tarea Y se sincroniza una fila en la bitácora, o mover todo a un solo lugar canónico?
9. Testing automatizado (el tip de Google). En ningún sprint vi tests Feature/Unit de Laravel como entregable — tu DoD es manual+tinker. ¿Querés incorporar tests automatizados generados desde el SPEC como criterio de cierre, o mantenerte en testeo manual E2E tuyo y dejar tests como opcional?
10. El prompt del Agente Capa 1. ¿Querés que te entregue un solo prompt maestro reusable que pegás al inicio de cada chat de planificación (que fuerza el modo, las preguntas, el glosario y la verticalización), o preferís dos prompts separados — uno para "iniciar proyecto nuevo / SPRINT" y otro para "feature sobre proyecto existente / SPEC"?
Respondé las que quieras en el orden que sea. Con eso armo la plantilla .md definitiva (SPEC + SPRINT + prompt Capa 1) lista para copiar-pegar, en tu formato, no uno genérico.

ANSWERS:
1. LA IDEA ES SIEMPRE MDs modulares entonces lo dividiria en 2 md , quizas md comunes y md modulares para escalabilidad de estas spec y vaya reajustandolo a mi gusto , 2.que sea dinamico osea el 70 % de casos un crud completo ya q tmb asi aprovechamos la ventan de contexto del agente ,pero quizas es una tarea muy critica o muy grande puede degradarse mas , 3. si hay clasificarla como epica 0 but both ways are ok ,pero si deberiamos en el 70 %de veces hacer login funcional , dashboard vacio , layout del sidebar con seed de opciones de menu . pues aprovechamos  a corregir todo el flujo . 4.si la verificacion tecnica siempre la hacen los agentes automaticamente , y el testeo por navegador yo lo hago manualmente ,ya que no gasto tokens en q los agentes lo hagan end 2 end / 5.que viva en el context.md por proyecto auto update por si el agente identifica algo que lo actualize,  6. solo hago mockup en 2 casos cuando es un proyecto de 0 para definir layout , paletas de colores , dashboard inicial, etc. o son nuevas feats complejas de explicar su valor agregado pero no es necsario para todo hacer mockups , la mayoria de  cruds no lo necesitas lo q yo hago para generarlos es ir a otro agente a parte y pasarle la spec o el sprint de la feat o del proyecto y empiezo a iterar hasta tener un design perfecto moderno quizas lo podemos estandarizar con este flujo el agent identifica que es sprint desde 0 entonces hace un prompt para geenrar el mockup iterativo osea con human in the loop nunca estricto o si identifica una feat compleja yo como engineering user le digo vamos a hacer un mockup de esta feat /spec y genera el prompt human in the loop para ese mockup+la spec, 7. yo reparto manualmente lo que ayuda es q al final de escribir o el sprint o la spec es una tabla resumen y yo asigno manualmente , 8. yo creo q un file bitacoradev.md seria lo indicado para no ensuciar el context con muchos tecnicismos por lo q este file bitacora sera muy importante tmb pero la idea es desagregar los md como arboles para filtrar lo que los agentes leen y mejorar su routing , 9.si creo que debemos incorporar ese consejo sobre todo con el desarrollo de apps backends que son apis q generalmente solo desarrollo pero no hay interfaz para testear ya que todo llega al frontend y no esta en mi scope ni tengo acceso al servidor de la empresa ,10. si seerian AGENTCONTEXTCAPA1SPRINTS (ESTE LO VOY A USAR PARA  PROYECTO DE 0  )Y AGENTCONTEXTCAPA1FEATS(PARA PROYECTO YA AVANZADOS QUE AHORA NECESITEN UN LOTE DE SPECS O FEATS(MAS DE 2 FEATS O QUE INVOLUCRE MAS DE 2 MODULOS DEL PROYECTO)) Y AGENTCONTEXTCAPA1SPECS(PARA FEAT PUNTUALES DE 1 A 2 ES MAS SPECIFIC) RECORDEMOS Q CAPA 1 NUNCA PROGRAMA SOLO ESCRIBE CONTEXT PROMPTS Y ME AYUDA A TOMAR DECISIONES DE ARQUITECTURA SU INPUT ES TEXTO , TRANSCRIPCION DE REUNIONES , IMAGES Y SIEMPRE PIDE FILES Q LE FALTEN A MI COMO ENGINEER GUIDE POR EJEMPLO ME DICE :"EJECUTA ESTA QUERY PARA DIAGNOSTICAR EN LA DB, PASAME ESTE FILE ,ETC"
OSEA LA IDEA ES UN KIT DE MD MODULABLES

P: El testeo de APIs sin frontend (tu punto 9). Para backends que son solo API y no podés abrir en navegador, ¿cómo querés que sea el DoD-Usuario?
R: BOTH:lTESTAUTOMATICOS + POSTMAN MANUAL NO AUTOMATIZAR MUCHO EL TESTING EN POSTMAN , PERO SI WRITE UN WALKTHROUGH  DE COMO TESTEAR EN POSTMAN SI ES QUE SE PUEDE YA Q  AVECES NO SE PUEDE Y QDA CONFIAR EN LOS TEST CODE

P: El mockup human-in-the-loop. Cuando Capa 1 detecta que toca mockup (proyecto de 0 o feat compleja), ¿qué genera exactamente?
R: Solo el prompt para el agente de mockup aparte
